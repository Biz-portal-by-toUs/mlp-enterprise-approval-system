server:
  port: 8090

spring:
  profiles:
    active: local

  servlet:
    multipart:
      max-file-size: 1000MB     # 개별 파일의 최대 크기
      max-request-size: 1000MB  # 한 번의 요청(여러 파일 포함 가능)의 총 최대 크기

# swagger 설정
springdoc:
  group-configs:
    - group: all
      paths-to-match: /**
